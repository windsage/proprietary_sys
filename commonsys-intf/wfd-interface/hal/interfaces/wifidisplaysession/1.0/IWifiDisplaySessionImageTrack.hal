/*==============================================================================
 *  @file IWifiDisplaySessionCb.hal
 *
 *  @par DESCRIPTION:
 *       HAL interface for Wifi Display Session Callbacks
 *
 *
 * Copyright (c) 2019 Qualcomm Technologies, Inc.
 * All Rights Reserved.
 * Confidential and Proprietary - Qualcomm Technologies, Inc.
==============================================================================*/
package vendor.qti.hardware.wifidisplaysession@1.0;

import IWifiDisplaySessionImageTrackCb;

interface IWifiDisplaySessionImageTrack {

    createMediaTrack(uint64_t trackObj, uint64_t clientdata, IWifiDisplaySessionImageTrackCb cb) generates(int64_t status, int64_t instanceId);

    encode(int64_t instanceId, handle input, sampleInfo info) generates(int32_t status);

    getTrackInfo(int64_t instanceId) generates (int32_t status, mediaInfo info);

    clearOverlays(int64_t instanceId) generates(int32_t status);

    destroyMediaTrack(int64_t instanceId);

};

